export function getFormattedDateTime(dateTime: string) {
    return new Date(dateTime).toLocaleString("en-US", {
        day: "2-digit",
        month: "short",
        year: "numeric",
        hour: "2-digit",
        minute: "2-digit",
        hour12: true,
    })
}